<UserControl x:Class="Luna.GUI.UserControls.ActionGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:inf="http://www.grandsys.com/luna"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UniformGrid Rows="1" x:Name="actionGrid">
        <Button Content="{inf:Resource Delete}" cal:Message.Attach="Delete"/>
        <Button Content="{inf:Resource Save}" IsEnabled="{Binding IsEditing}" cal:Message.Attach="Save"/>
        <Button Content="{inf:Resource Cancel}" IsEnabled="{Binding IsEditing}" cal:Message.Attach="Cancel"/>
    </UniformGrid>
</UserControl>
